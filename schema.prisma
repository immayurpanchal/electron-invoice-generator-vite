datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model products {
  id           Int    @id @default(autoincrement())
  product_name String
}

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native"]
}
